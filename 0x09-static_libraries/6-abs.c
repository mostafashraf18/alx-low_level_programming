#include "main.h"
#include <stdio.h>


/**
 * _abs - Entry point
 * @i: n is an integer
 * Description: prints the absolute value of an integer
 * Return: int
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
